# Sample Shipment Documents

Put local test PDFs and images in this directory when using `POST /api/trigger/simulate`.
The simulate endpoint only reads filenames from this directory and rejects absolute paths or path traversal.
